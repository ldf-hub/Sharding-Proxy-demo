spring:
  application:
    name: sharding-proxy-demo
  profiles:
    active: dev
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://101.34.45.104:3321/readwrite_splitting_db?characterEncoding=utf-8&useSSL=false&allowPublicKeyRetrieval=true
    password: root
    username: root

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

